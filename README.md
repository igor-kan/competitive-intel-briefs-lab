# competitive-intel-briefs-lab

Generate competitive intelligence briefs and action recommendations from structured competitor data.

## Quick start
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python -m src.run --input examples/competitors.csv --output out
```
